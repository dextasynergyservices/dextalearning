# Normalize all text files to LF in the repo (prevents CRLF churn on Windows
# checkouts that Biome would otherwise flag). Binary assets are left untouched.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.mp4 binary
*.woff binary
*.woff2 binary
