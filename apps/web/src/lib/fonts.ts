// Brand typefaces (blueprint §9.4). Imported for their side effects so the
// @font-face declarations are bundled and the families referenced in the
// Tailwind @theme (index.css) resolve.
import "@fontsource/righteous/400.css"; // display / hero / wordmark
import "@fontsource-variable/dm-sans/index.css"; // body / UI labels
import "@fontsource-variable/space-grotesk/index.css"; // stats / data / badges
import "@fontsource-variable/jetbrains-mono/index.css"; // code (Tech Academy)
